# Dispatch report — it-f6c2: the silent call meets its ledger

Eighth worktree dispatch (serial run). Agent report condensed faithfully;
dispatcher's landing addendum follows.

## Outcomes against the RETURN SPEC (three constructed ends, all MET)

1. **RETURN spec slot** — `render::brief` renders `framings::USER_OWNED_SLOT`
   between the outcomes line and REFLECTIONS: the report carries a section headed
   "user-owned calls:" naming each call with its filed thread, or the single word
   none; the slot states that a report missing the section is a flag, and that
   code is not a landing for a user-owned call.
2. **Harvest reconciliation** — `render::harvest` prints the mechanical compare
   under GRAPH ACTS: `queries::latest_report_doc` joins the seat to the
   registered report (kind=report, supports edge, newest wins — a re-dispatch
   reconciles against its own arc's report); `queries::declared_user_owned_calls`
   is the one parse point (None = section absent = the flag; Some(0) = "none";
   Some(n) = declared). The gap line confronts as a question, never a verdict;
   before a report exists, the await arm prints the badge-thread count against
   the prose in hand and teaches the re-run.
3. **First-interception echo** — the first-badged-write echo carries
   `framings::USER_OWNED_ECHO`: user-owned calls land as threads, never as code
   shape. Fired live on the agent's own first edit.

## Test results (verbatim, read raw)

```
test result: ok. 125 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.36s   (tests/basic.rs)
test result: ok. 2 passed; 0 failed (broken_pipe) · 1 passed; 0 failed (observed_set) · 1 passed; 0 failed (store_lint) · 1 passed; 0 failed (surface_lint) · 2 passed; 0 failed (worktree_proof)
```

New pins: `return_spec_and_first_echo_carry_the_user_owned_calls_rule`,
`declared_user_owned_calls_parses_the_section_shapes`,
`harvest_reconciles_declared_user_owned_calls_against_badge_threads`.

## Mechanics

- Branch `worktree-agent-aca903461b7b39612`, commit `eb991f7` ("The silent call
  meets its ledger: the slot declares, harvest reconciles, the echo names the
  thread"), 5 files, +287. Fast-forwarded at landing; SKILL.md regeneration ran
  and was a no-op (echo strings live in framings, not the guide text).
- Graph acts under the badge: vein cl-cjbb (`user-owned-accounting`, source
  file:src/queries.rs, supports it-f6c2); affirm cl-enu7 --to file:src/render.rs
  (re-read against the extension); thread th-cqyg (the declared call below).

## user-owned calls:

- the harvest report channel: whether `q harvest` grows a `--report <path>` arm
  or the homework order flips — filed as th-cqyg, awaiting the user.

## Agent reflections (verbatim highlights)

- The report-channel seam surprised me: the settled fix presumes a read the seat
  cannot make until registration. The await line is honest but the mechanical
  compare only fires on a harvest re-run — th-cqyg holds the fork.
- The parse is deliberately heading-strict: prose like "No user-owned calls were
  encountered." reads as section-absent and flags — over-asking chosen, since
  under-flagging is the defect this item exists to kill.
- M counts all badge threads, not just user-owned ones — hence "a question, not
  a verdict"; an evidence thread will show as an undeclared gap.
- Pleasing self-reference: this arc exercises its own mechanism at the
  dispatcher's seat.

## Dispatcher's landing addendum

Eighth consecutive worktree arc, fast-forward again, dc-g5x5 clean again, zero
fork friction reported. The self-test ran live at this seat: pre-registration
harvest rendered the await arm (1 badge thread, no report to parse); this
registration makes the next harvest parse this very section — N=1 declared,
M=1 filed, reconciled. Composed strings (slot, echo, reconcile/await lines) join
the dc-vzvf ratification pile.
