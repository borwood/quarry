# Dispatch report — it-7ss9 "q dispatch and the leaseless observation surfaces"

Authored by the dispatched build agent (claude-fable-5 subagent), 2026-08-10.
Registered at harvest by the dispatcher. Dispatcher addendum at the end.

## 1. OUTCOMES (against the six RETURN-spec lines)

**1. `q dispatch` performs brief+reserve+in-flight+payload as one act, refuses unbound with a session-adopt pointer, carries QUARRY_DISPATCH in the payload.** `ops::dispatch` (src/ops.rs:559) orchestrates: brief event logged (C8's record) → lease reserved (`--files`, falling back to the item's `write_set`; foreign-holder, settled-status, and no-globs refusals each teach the corrective verb) → `status=in-flight` → machine-local badge state saved carrying the contract (globs + acceptance + log cursor) → dispatch event logged → brief rendered **after** the lease so the payload shows the live write-set, not the pre-dispatch void (a bug caught mid-build). CLI arm at src/main.rs:1195. Scratch transcript: unbound run printed the adopt-pointer error; bound run printed the paste-whole payload with `QUARRY_DISPATCH=it-3hm7` in pwsh and bash forms. Tests `dispatch_one_act_then_harvest`, `dispatch_refuses_settled_and_foreign_lease`.

**2. Guard accrues machine-locally with no graph load on the write path; first badged write echoes the contract; leaseless threshold nudge fires once with a derived item match.** Accrual is append-only JSONL (`graph/.touched.jsonl`), two keys (`item:<id>` / `session:<name>`): src/coord.rs:339-356. `teach::observe_write` (src/teach.rs:505) rides the guard after allow/warn and never denies. Steady write path reads two small machine-local files, zero `load_all()`; the single graph load lives inside the nudge, which fires exactly at the threshold crossing (distinct counts only grow — once per session by construction). The contract echo reads the dispatch state captured at dispatch — no graph there either. **Per the dispatcher's mid-task note: the lease layer now judges repo-relative paths only.** The guard arm (src/main.rs:699-717) engages lease_check + observation only when the write target resolves inside the host repo root, with a component-boundary strip (also killing a `repo2/` prefix false-match); `lease_check` itself early-allows out-of-repo shapes (src/teach.rs:455), and `observe_write` skips them. Verified live: the exact scratchpad path through `q hook guard` is silent, exit 0, nothing accrued; in-repo writes behave as before. Live dogfood: my own edits accrued in this repo's graph/.touched.jsonl from the first rebuild onward. Scratch: third distinct file nudged; first badged write echoed the contract; a badged write outside the lease denied (containment intact). Tests: `observe_write_contract_echo_and_drift`, `observe_write_leaseless_threshold_nudge`, `dispatch_state_and_touched_accrual`, extended `c8_briefed_gate_and_lease_check` (out-of-repo Allow for holders, foreigners, unbound, and badged).

**3. Verbs stamp the badge; a dispatch-writes query exists, advertised in harvest.** `Store::log_event` stamps `"dispatch"` env-first with state-file fallback (`coord::current_dispatch_badge`, src/coord.rs:323). Real-repo evidence: the rider mint logged `"dispatch":"it-7ss9"` via env transport. `q query dispatch <item>` (src/render.rs:506) renders stamped events + accrued files; harvest advertises it. Tests: `log_events_stamp_the_badge_from_state`, trace assertions.

**4. Landing prints observed-vs-leased and report homework; spine_check consumes observed; wrap lists leaseless files and unharvested dispatches.** `q harvest` (src/render.rs:423, arm src/main.rs:1218): observed files with outside-lease markers, leased-but-untouched globs, badge-stamped act counts, the RETURN spec, one-command report registration, landing commands; clears the badge. `spine_check` (src/main.rs:460) prefers the observed set. Wrap: leaseless pickup (delivered once, then cleared, with `items_matching_files` resemblance) and `unharvested_dispatches` (src/queries.rs:454). All exercised in the scratch transcript, including the unharvested-dispatch wrap flag and clean state after release.

**5. Brief renders spine bodies, a dispatcher-addressed behind check, reflections/stop-report RETURN; rider exists on=brief.** All in src/render.rs: spine bodies on the shelf; `DISPATCHER — BEHIND CHECK` with per-edge `q affirm` commands; backlinks ("who leans on this landing"); REFLECTIONS + STOP-REPORTS lines; the stale flip-done actor rule replaced with report-and-stop plus `q harvest`; prior report docs render as evidence with path. Non-blocking C3 wording per the dispatcher's note (queue the thread, take the least-committal provisional path, keep building, flag at report). Rider **do-799m** minted via q verbs (kind=protocol, on=brief, tier=inline, about ar-xa38), body carrying the updated build rules (durable cargo config); verified rendering inside the it-7ss9 brief. Test: `brief_carries_dispatch_citizenship_sections`.

**6. Tests pass, read raw; chain exercised end-to-end.** Final raw line, unfiltered: `test result: ok. 39 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.60s`. End-to-end ran in B:\tmp\quarry-scratch through the real hook entry point: leaseless accrual → nudge → wrap pickup → unbound refusal → dispatch → contract echo → containment denial → badge-stamped claim → unharvested wrap flag → trace → harvest → one-command report registration (`--supports` edge; re-rendered brief carried the report) → dispatcher landing with badge/touched cleanup. `q init --claude` re-run regenerated the skill with the new guide passage; `q view` regenerated.

## 2. NOTABLE CALLS

- **Badge transport: both channels, env-first.** Empirically confirmed the hook-env hypothesis: the agent shell's `QUARRY_DISPATCH=it-7ss9` never reached the guard — the agent's edits accrued under `session:quarry` because no state file existed for this pre-verb dispatch. At harvest of it-7ss9, observed-vs-leased says "no code writes observed under this badge" — the transport gap surfacing, not a harvest bug; the session-keyed accrual and the git diff carry the record. Accepted blur documented in coord.rs — and it is harder than blur: while a state-file badge is active, a main-session write outside the dispatched lease is denied. One badge at a time is enforced (`q dispatch` refuses over an active foreign state, naming `q harvest`).
- **Threshold: 3 distinct source files.** One is a fix, two a tweak, three an arc. Wrap-clearing means a post-wrap arc can nudge again — treated as a feature.
- **Payload shape:** dispatcher confirmation lines, then a marked HAND-OFF block: badge instruction (both shells, with the why of the state-file fallback) followed by the full derived brief.
- **Drift notice** read as "the dispatched item moved mid-flight": cursor planted at dispatch, checked on badged writes throttled to 120s, delivered once. Never a per-write log scan (same watch-listed whole-log-read compromise as the alert check).
- **`--supports` on `q new`** makes report registration genuinely one command; **`write-set+=` on `q set`** lets shaping declare expected write-sets, feeding the `--files` fallback and the nudge's derived match.
- **Repo-relative contract (dispatcher note):** enforced at three layers — guard arm skip, lease_check early-allow, observe_write skip — so neither warn/deny nor accrual ever judges an out-of-repo path.
- **Not committed.** Harvest judges the diff; landing and the commit are the dispatcher's.

## 3. REFLECTIONS (the agent's own words)

- The state-file fallback rescues observation but hardens containment for the dispatcher's own hands (deny, not blur, outside the dispatched lease while a badge is active). Tolerable single-orchestrator; two concurrent dispatched agents on one machine do not fit the single-slot state file. If parallel dispatch matters later, per-chat badge state keyed by the binding the hook already knows is the likely shape.
- The scratch nudge said "no item covers these" with the obvious item sitting right there — the derived match is only as good as declared write-sets/file-edges, and fresh sketches rarely have either. `write-set+=` exists now; whether shaping actually declares them is a habit the graph can't force. Watch which arm of the nudge fires more.
- The dispatcher's own graph events during an active dispatch get badge-stamped into the trace (a `wrap` event appeared inside it-3hm7's trace). Honest but noisy: the trace is "everything on this machine under the badge," not "everything the agent did."
- I'm not fully sure serializing dispatches ("refuse while another is active") is right for pipelined hand-offs; the error names `q harvest`, so the path is discoverable, but it constrains you.
- The area-first-touch gate ate my first scratch run's item mint because I skipped the read-first — the system catching its own test author was the most persuasive dogfood of the day. Likewise the dispatcher's scratchpad-warn catch: the noise class existed before my change (the old guard judged absolute paths too), but accrual would have amplified it; killing it before habituation was the right call.
- Dispatcher homework outside my lease: add `graph/.touched.jsonl` + `graph/.dispatch.json` (or `graph/.*`) to this repo's .gitignore; docs/DESIGN.md has no dispatch-chain section yet.

## 4. DISPATCHER ADDENDUM (at harvest, 2026-08-10)

The report's test claim held only in a clean environment. Independent verification in the dispatcher's **bound** chat (QUARRY_SESSION injected — this repo's normal state from now on) failed 2 of 39: `session_injection_binds_and_rewrites` and `area_watermark_lifecycle`, both inheriting the injected session identity into test processes (injection no-ops over an existing env session; watermark keys shift). Clean-env rerun passed 39/39, confirming env sensitivity rather than logic error. Fixed at harvest in the test fixture: `temp_store()` now scrubs `QUARRY_SESSION`/`QUARRY_DISPATCH`. Post-fix, bound-env run: `test result: ok. 39 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.70s`. The miss is instructive, not culpable: the agent could not have seen it — the failure requires the bound-chat environment that only the dispatcher's harness had. Harvest-time independent verification catching what the agent's environment could not show is the dispatch-citizenship split (dc-kpqg) doing its job.
