# DISPATCH REPORT — it-pgn9 "vein claims mint kindless: the vein prompt is missing at land time"

## Outcomes (the RETURN spec)

**`vein-prompt` — LANDS.** A kindless claim whose title leads with a registered name now draws the vein species question at every mint surface, settle command in hand. The mechanism: `queries::backtick_titled` reads the title's leading backticked span under the dc-qvtz deliberate-name floor (2–60 chars; the name must *lead* — mid-title backticks draw nothing); `print_mint_surfaces` in src/main.rs consumes it beside the existing dc-6gn9 method prompt. All three surfaces — q claim, q new, q rule — already funnel through that one prompt point, so coverage is by construction. Prompt, never a gate: the ✔ mint line prints first, the engine validates nothing (dc-yd9s). Method: predicate pinned by test `backtick_titled_reads_the_leading_name_under_the_deliberate_floor`; live-verified in a scratch store — kindless backtick mint drew the question + settle command; a `--kind vein` mint stayed silent; a method-carrying kindless mint kept the reading/measured question (the two questions never stack, else-if lane).

**`land-time-ask` — LANDS.** `render::harvest` now enumerates the badge's kindless backtick-titled mints (`queries::kindless_backtick_mints`, same predicate) under `framings::vein_ask_line`, each with its per-claim settle command, positioned before the LANDING lines. Method: test `harvest_asks_the_kindless_backtick_mints_before_ratification_passes_them_by` proves the full kill of the silent-repair path: the ask fires for the cl-ybth shape; plain-titled, unstamped, and kinded mints stay out; the ask precedes LANDING; `ratify_landing` alone leaves the mint asserted; settling `kind=vein` routes the mint onto the assay ladder on re-harvest and the ask falls silent. The dispatcher is confronted with the settle command at the judgment seat — repair happens loud, in hand, never silently.

Suite: `test result: ok. 98 passed; 0 failed` (basic.rs) + surface lint green (read raw, unfiltered). Release binary rebuilt.

## Composed strings and interpretive calls (dc-vzvf channel — for ratify-or-amend)

- **`framings::VEIN_PROMPT`**: "this claim leads with a registered name - a mechanism or mandate read off landed code (--kind vein), or the receipt of a landed capability (--kind feature)? Kindless claims never ride the assay ladder."
- **`framings::vein_ask_line(n)`**: "N kindless mint(s) under this badge lead with a registered name - the assay ladder rides species, so landing now passes these by asserted. Settle each, then land:"
- Interpretive calls: (1) the prompt offers only the two ladder-riding species (vein/feature), not contract — contracts also title backtick-first, but the prompt exists because kindless never ratifies; the settle command accepts any kind string regardless. (2) Method-prompt wins the lane when both shapes hold. (3) The harvest ask fires only on asserted, unarchived badge mints. (4) The solo done-flip got **no** ask — the spec named harvest; a symmetric ask there is a cheap follow-up if wanted. (5) A kindless claim retitled into the shape after mint is still caught at harvest (current title is read), but draws no prompt at `q set title=` — set is not a mint surface.

## Graph writes

- **cl-sv7z** vein `vein-prompt` (source file:src/main.rs) and **cl-vuda** vein `land-time-ask` (source file:src/render.rs) — titled to the acceptance names so intent-delta joins; both `supports → it-pgn9`.
- Affirms on what the fix re-verified: cl-9z95, cl-enu7, cl-baxa, cl-h3r8 (re-read against the diff), cl-c34g/cl-en99/cl-qm6t (instrument re-run green), cl-az38 (nothing behind).
- ~30 further sev-4 drift entries remain in `q query behind` — the additive-landing wave over the four touched files; the agent affirmed only what it actually re-read, the rest is boundary review (th-ybv9 is already watching this recurring-noise shape).

## Reflections

The fix was almost entirely assembly from standing veins — the predicate floor from dc-qvtz, the prompt point from cl-9z95's landing, the mint-filter shape from `assayable`, the framings channel — which is the system working as designed, and also means the genuinely new surface area is two strings and one predicate. Two frictions: the surface lint caught raw `front.title` reads immediately (good tripwire); and the return spec's "every mint surface (q claim, q new, q rule)" is satisfied structurally by the shared prompt point, but q rule mints *decisions*, so the claim-shaped prompt can never actually fire there — the spec inherited that phrasing from cl-9z95, which has the same property. One doubt for the dispatcher's eye: the vein prompt fires on *any* kindless backtick-led claim, including would-be contracts — under-offering species was judged better than a three-way question; amendable in one string.

---
Harvested by the dispatcher 2026-08-17: suite re-run and read raw (98+1 passed, 0 failed), both surfaces judged live against the tests that pin them. **Observed-set miss, instance three (it-bj3b):** the harvest's observed list said tests/** untouched while git shows tests/basic.rs modified under this arc — the same miss as the assay-office run; instances now alternate (runs 3 and 9 missed, runs 4–6 observed), which is a diagnostic clue for the fix. Under-offering species in the vein prompt confirmed as the right call — a wrong-kind settle is loud and correctable, a three-way prompt is noise on every mint. Landed done; both mints ratified at landing.
