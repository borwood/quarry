# Dispatch report — it-xzp7, vein rename sweep

Agent report, delivered 2026-08-15, registered verbatim by the dispatcher.

**`vein-rename` — LANDED.** Every surface in src/** and tests/** now says vein where it said spine; zero `spine`/`bones`/`bedrock` matches remain in either tree (case-insensitive). The three bones surfaces carry the dc-eq9d ratified lines verbatim: area-open backlink line and actor-rules build-from line in src/render.rs, guide landing paragraph in src/teach.rs; the adjacent shelf comment in render.rs folded bones→veins the same way. Bare swaps everywhere else, all from the reviewed list: shelf label `vein:`, `register its vein while the diff is warm`, `vein or no vein?`, the vein register, vein extraction and presence, plus `spine_check` → `vein_check` (definition, both call sites, comments) in src/main.rs and the `spines` → `veins` variable in src/queries.rs. Tests migrated in tests/basic.rs (`vein_presence_check`, fixtures, assert messages, comments). No swap produced an uncovered sentence — nothing was invented. Verified: raw `test result:` lines read 0+0+66+1+0 passed, 0 failed across all five binaries; release rebuilt; `q init --claude` regenerated SKILL.md (vein language present, no residue); live renders of area-open, the brief shelf, and ACTOR RULES all show the ratified lines.

**`shelf-by-claim-kind` — NOT built here, per dc-gaa9.** The line subsumes into the renderer item (it-wcwd) and judges satisfied when the renderer lands the capability under that name. Interim state, expected and flagged: the flat shelf label reads `vein:` over all live claims, including the four `claim·measured` ones — the truth is visible in each entry's atom (`claim·vein` / `claim·measured`), and the per-species shelf resolves it when it-wcwd lands.

**`claim-kind-pass` — LANDED, for harvest review.** All 34 claims now carry explicit kinds via `q set <id> kind=… --note "claim-kind pass under dc-yd9s (it-xzp7)"`: 30 vein (29 set; cl-ybth already carried it), 4 measured (cl-c34g, cl-en99, cl-qm6t, cl-eb8j). Judgment calls to review: **cl-eb8j** (enforcement-beats-doctrine) is an autopsy-sourced observation, not a code-read mechanism — assigned measured as a one-off reading (the sediment branch of th-cfmt), the least-committal fit; **cl-3qwu** (surface-atom) and **cl-3rx9** (session-kind-field) are contract-adjacent (symbol-grain flavor) but were minted as landed-capability landmarks, so they kept vein per dc-wrnc's rename-of-the-species. The three superseded claims (cl-amza, cl-farc, cl-vsew) were kinded too — existing claims all carry kinds; they never render on the shelf. No claim was assigned feature or contract; those species start populating with new mints.

**Operational notes.** The release rebuild required stopping the long-running `q serve` (PID 34176, held the exe lock); restarted detached and probed 200 on port 7171 — the view tab is answering. `q view` re-baked the offline courtesy file. Working tree left uncommitted for harvest: 5 code/test files, 33 claim nodes plus log (q-verb writes), regenerated SKILL.md.

**Reflections.** (1) The kind-pass bumps every claim's version, so everything citing them at pinned versions is now one version behind — a graph-wide low-severity behind ripple that harvest/affirm will meet; there was no bump-free channel for a frontmatter-only kind set, and maybe there should be one (kin to q unlink's no-bump precedent — a kind assignment is bookkeeping about the node, not new content). (2) The exe-lock collision between `q serve` and rebuilds feels like a repeatable snag for any teach.rs dispatch on this machine — the protocol doc warns about hooks mid-build but not the serve lock. (3) Historical claim titles still say "spine" (cl-gu5f, cl-hd56, cl-jgnj) — read "historical node bodies stand as written" as covering titles and left them; if titles were meant to migrate, that's a cheap follow-up but it would bump and cascade. (4) The ratified area-open and actor-rules lines dropped the backticks the old lines carried (`load-bearing bones`, `load-bearing capabilities`) — shipped verbatim without backticks per dc-eq9d; worth a glance at harvest since backticked spans feed the lexicon join.

**Dispatcher verification at harvest (2026-08-15):** residue grep clean (zero spine/bones/bedrock in src/ and tests/); the three ratified lines confirmed verbatim at render.rs:197, render.rs:471, teach.rs:139; full suite re-run by the dispatcher's own hand — test result lines read 66 passed and 1 passed, 0 failed. Backtick reflection judged: the lexicon joins run over node titles and bodies, never over render output strings, so the dropped backticks in CLI lines carry no lexicon effect — no change needed.
