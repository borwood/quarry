# DISPATCH REPORT — it-4p7m: the source-drift pass

## Against the RETURN spec

- **behind carries zero file-drift wants-action entries at pass close** — met. `q query behind` now prints "nothing behind — every ref current." (The queue had grown to 63 entries from the filed 57 by pass start; all 63 are cleared.)
- **every drifted claim shows a recorded affirm, edit, or refute in its log** — met. All 63 entries received a recorded act: 60 resolved by affirm after re-reading against the current blob; 3 were amended (edit + note + affirm): **cl-h3r8** (body said the vein framing ships *without* the assay sentence — it landed with it-swsy), **cl-3qwu** (Atom gained the weight field; atom_unpack_html's deletion actually strengthened its only-renderers line), and **cl-p6aj** (retitled too: dispatch no longer renders/prints a brief payload — it logs the brief event and mints a join token into a one-line spawn prompt, per dc-zbxj). **Zero refutes** — no claim asserted a mechanism the code no longer keeps.

## Method

Per file: each stamped blob diffed against the current blob (`git diff <old> <new>` on blob ids), each claim body read via `q open`, and the named mechanism verified in the current source by symbol — not by trusting the diff alone. Verified live among others: the join-gate teach line (teach.rs:597), badge_for's never-the-holding-chat resolution, ratify_landing, consume_readings, the acceptance-gate tripwire, the lexicon floors (backtick 2–60, sig_tokens ≥5, reverse pass ≥6), affirm_teaching's instrument/manual split, and charter-at-wake on both surfaces.

## Graph writes

3 edits + 1 retitle + 60 single/multi-ref affirms under the badge, plus one queued thread — **th-ybv9**: settled-strata drift — archived, dropped, and superseded *sources* (it-u7dp ×3, cl-amza, cl-farc, cl-vsew) re-enter behind as sev-4 wants-action on every src move; dc-6gn9's sediment carve-out covers readings only, so these will need re-affirming every pass. The classifier boundary is a user ruling, so the call is queued, not taken.

## Reflections

(1) Affirming a superseded claim felt mildly dishonest — the re-stamp says "reviewed at this blob" about text that by definition no longer describes the code; th-ybv9 carries that doubt. (2) One stamped blob (63656ac, it-5j34's) was unresolvable in the object store — likely stamped against a dirty working tree; judged against the current file instead, which worked, but a stamp that can't be diffed later weakens the drift instrument. (3) The pass was almost all affirms because the drift window was additive landings plus the register scrub — the sev-4 lint is doing its job as a review trigger, but at 63 entries per landing wave the affirm burden scales with claim count; the multi-ref affirm (one verb restamps all of a node's drifted edges) was the only thing keeping it tractable. (4) `q edit`'s citation echo catching new mentions at write time is a genuinely good surface — it caught nothing wrong here, but every echo was read.

---
Harvested by the dispatcher 2026-08-17: behind verified empty by the dispatcher's own query; the retitled cl-p6aj re-read against the dispatch mechanism this session exercised eight times — accurate. 64 badge events, zero file writes (the graph-only lease was fictitious again — it-kjgz's case strengthens). The settled-strata classifier question is correctly queued (th-ybv9), not taken. Zero-mint arc: nothing to ratify. Landed done.
