══ DISPATCH BRIEF — "fire-time liveness: dispatch offers the hand-off when a dispatching session is live" — item [ready] v9 · cli · assistant (it-hapc) ══
derived from the graph at render time; if this brief reads wrong, the graph is wrong — fix the graph, re-render.

THE WORK:
  Reshaped 2026-08-14 under dc-crea [decision in-force: `fire-time routing: a pull, never a send; the only choice is which dispatcher to wake` — areas: process, cli] (pull semantics): at q dispatch from a non-dispatch session, derive the live sessions with appropriate coverage for the item (kind field from it-skpa [item done: `session kind as registry data: --kind on set, one validation point, open set`] + purview fit + last_seen heartbeat). Any live - offer the leave: do not fire solo, keep the item ready (ready IS the dispatcher feed), print which live session(s) cover it; no choosing among them - the first to claim dispatches it, and the claim point guards the race (dc-qyr5 [decision in-force: `multi-held dispatch: a chat holds many, an item belongs to one chat, the boundary harvests all`]). None awake - offer the wake choice: enumerate registered dispatch-kind sessions covering the item; one charter-certain candidate - offer its launcher (dispatcher-session.cmd today); several plausible - the user picks. Fire solo stays legitimate throughout. A surface, never a gate.

READ-FIRST (cited at pinned versions — q open <id> for any neighborhood):
  · depends on "single-slot badge: parallel dispatch does not fit the machine-local state" — item·debt [done, archived] v7 · cli · assistant (it-u8uf)
      One dispatch state at a time (do-xt6f [doc registered: `dispatch report: the dispatch chain lands`]): q dispatch refuses while any dispatch is active on this machine, serializing hand-offs across every chat. Promoted from watch to debt 2026-08-12 by dc-ydvb [decision in-force: `session kinds: decisions and dispatching, conventional first; dispatch is a hand-off; parallel is the normal shape` — areas: process]: this is a regression, not a future concern — the user reports the single slot was introduced and first noticed in a session that had been running multiple parallel agents until that point, because parallel is the normal shape ("are these parallelizable items? fire them all off"). The ruling makes per-chat badge state a precondition of the settled dispatch pattern: a decisions session firing hot while a dispatching session stewards in-flight work collides at this slot today. Likely shape, unchanged from the watch: dispatch state keyed by the chat binding the session hook already knows; refusal only guards a second dispatch from the same chat; the write guard, event stamping, trace, and harvest resolve the badge for the acting chat, not the machine. Adjacent: it-9p3v [item done: `badge trace is machine-scoped, not agent-scoped`] (the trace is machine-scoped — the same key fixes both).
  · depends on "session kinds: decisions and dispatching, conventional first; dispatch is a hand-off; parallel is the normal shape" — decision [in-force] v2 · process · user (dc-ydvb)
      Two session kinds, conventional first: decisions (rulings, design, the thread queue) and dispatching (ready consumption, in-flight stewardship, harvest, homework residue). Kind lives in the charter until orientation consumes it - structure follows kind-on-consumption. A decisions session firing hot is legitimate dispatch: the preferred flow hands ready work to a live dispatching session; when none is live, offer to launch one (launcher script) or fire solo. q dispatch is definitionally a hand-off - building in-loop rides the solo path (brief + reserve), never a silent fusion. Parallel dispatch is the normal shape ("are these parallelizable items? fire them all off"); the single-slot badge is a regression against it, and per-chat badge state (it-u8uf [item done, archived: `single-slot badge: parallel dispatch does not fit the machine-local state` — areas: cli]) is a precondition, promoted to debt.
  · depends on "session kind as registry data: --kind on set, one validation point, open set" — item·slice [done] v5 · cli · assistant (it-skpa)
      Lands the field dc-ad8b [decision in-force: `session kind is a field: design | dispatch; the set stays open` — areas: process] rules: registry entries carry kind (design | dispatch), set at q session set --kind, shown by q session list and the wake surfaces beside the charter. The set is OPEN per the ruling: kind parses and validates in ONE place, surfaces render the kind they find rather than branching two ways, and adding a third kind is a data change plus one match arm, never a sweep. First machine consumer is it-hapc [item ready: `fire-time liveness: dispatch offers the hand-off when a dispatching session is live`] (fire-time liveness derives whether a dispatch-kind session is live); it-sumw [item done: `orientation consumes the charter: wake surfaces carry the session's kind`] already renders charter at wake and picks up kind rendering when the field exists. Kindless registry entries stay legal and render as today.
  · depends on "fire-time routing: a pull, never a send; the only choice is which dispatcher to wake" — decision [in-force] v4 · process, cli · user (dc-crea)
      Refines the dc-ydvb [decision in-force: `session kinds: decisions and dispatching, conventional first; dispatch is a hand-off; parallel is the normal shape`] hand-off; tightened 2026-08-14 in discussion, replacing this decision's earlier "send" phrasing: routing is a PULL, never a send. At q dispatch from a non-dispatch session, derive the live sessions with appropriate coverage for the item (kind + purview fit + last_seen). Any live and appropriate - do not fire solo: leave the item in ready (ready IS the dispatcher feed; the kind-shaped wake and the purview-change surfaces deliver it) and inform the user which live session(s) cover it. There is never a choice between live dispatchers: the first to claim dispatches it, and the claim point already guards the race (dc-qyr5 [decision in-force: `multi-held dispatch: a chat holds many, an item belongs to one chat, the boundary harvests all`]: an item belongs to one chat once dispatched; a second chat needs --steal --reason). Routing is advisory, never binding - no routed-waiting state; an unclaimed item stays honestly ready for anyone. The ONLY who-choice arises when no appropriate dispatcher is awake and design must choose which to WAKE: pick by charter certainty and inform the user; any ambiguity defers to the user. Fire solo stays legitimate throughout. Remains a surface, never a gate.
  · area "cli" — area [active] v1 · assistant (ar-c7f5)
      The q binary: verbs, queries, rendering.
      open thread: "measured species: one-off readings vs living measurements - behind must tell sediment from rot" — thread [queued] v1 · cli · user (th-cfmt) — NOT yours to settle
      open thread: "setup runbook: what quarry asks and offers at init time" — thread [queued] v4 · cli · user (th-qv27) — NOT yours to settle
      open thread: "the brief is a dump: selection, hierarchy, and dedup for the dispatch payload" — thread [queued] v1 · cli · assistant (th-uacu) — NOT yours to settle
      open thread: "worktree subagents: stamping does not survive the worktree boundary" — thread [queued] v1 · process, cli · user (th-xeqw) — NOT yours to settle
      open thread: "the file-size infection: mid-task warnings fail; what defense actually works" — thread [queued] v4 · cli, process · user (th-yzmj) — NOT yours to settle
      open thread: "bundled work: a parent item with part-of children; the payload presents the bundle" — thread [queued] v1 · cli, process · user (th-zzqv) — NOT yours to settle
      decision in force: "implementation is Rust; the binary is q" — decision [in-force] v1 · cli · user (dc-5kxt) — 
      decision in force: "the view is a render, not a record: graph/view stays untracked" — decision [in-force] v1 · cli · user (dc-5pb3) — User ruling 2026-08-12 (ui-cleanup session): the rendered page graph/view/index.html is a derived artifact — no reason whatsoever to git-track it. Already the implemented reality (.gitignore carries graph/view/); this node makes it policy, not accident: any machine renders its own with q view, and no fix to a stale or missing page ever involves git. Grounds stated with the ruling: most of quarry's UI is renders of verb responses — the page is a render too, never a record.
      decision in force: "fire-time routing: a pull, never a send; the only choice is which dispatcher to wake" — decision [in-force] v4 · process, cli · user (dc-crea) — Refines the dc-ydvb [decision in-force: `session kinds: decisions and dispatching, conventional first; dispatch is a hand-off; parallel is the normal shape`] hand-off; tightened 2026-08-14 in discussion, replacing this decision's earlier "send" phrasing: routing is a PULL, never a send. At q dispatch from a non-dispatch session, derive the live sessions with appropriate coverage for the item (kind + purview fit + last_seen). Any live and appropriate - do not fire solo: leave the item in ready (ready IS the dispatcher feed; the kind-shaped wake and the purview-change surfaces deliver it) and inform the user which live session(s) cover it. There is never a choice between live dispatchers: the first to claim dispatches it, and the claim point already guards the race (dc-qyr5 [decision in-force: `multi-held dispatch: a chat holds many, an item belongs to one chat, the boundary harvests all`]: an item belongs to one chat once dispatched; a second chat needs --steal --reason). Routing is advisory, never binding - no routed-waiting state; an unclaimed item stays honestly ready for anyone. The ONLY who-choice arises when no appropriate dispatcher is awake and design must choose which to WAKE: pick by charter certainty and inform the user; any ambiguity defers to the user. Fire solo stays legitimate throughout. Remains a surface, never a gate.
      decision in force: "the view renders per request: no baked freshness contract" — decision [in-force] v1 · cli · user (dc-f79h) — User ruling 2026-08-12 (ui-cleanup session): the view is rendered per request — the user keeps a long-lived tab, and a baked file lies to it between boundaries. Serve mode wraps the existing render in a request loop; no new dependency warranted. Completes dc-5pb3 [decision in-force: `the view is a render, not a record: graph/view stays untracked`]'s tense: a render, not a record — now in time as well as in git. The boundary regens and the baked file remain as courtesy for q view --open, demoted from freshness contract to cache.
      decision in force: "spine landmarks: land-time extraction, presence prompts, never gates" — decision [in-force] v2 · process, cli · user (dc-grrb) — Ratified 2026-08-09 with amendments. A spine is a CLAIM read off landed code: blob-stamped file source (drift detectable), falsifiable (refute + blast when a refactor kills the capability), reliance-derived weight (supports edges from spine to the features standing on it — load-bearing is a blast radius, not a label), superseded in chains as capability evolves. Supply is constructed at landing: the done-flip and release run the write-set-uncited check — PRESENCE of citation, never quality; a prompt, never a gate, because a blocked done breeds Goodhart claims. Feature acceptance may carry the landmark via protocol rider; wrap backstops with the landed-uncited lint. Delivery: the brief grew a spine shelf per area; q open on an area names build-from-spine when claims are present. Spine titles are written in the project's naming register — titles feed the relatedness lexicon, so landmarks surface themselves to future work. Landing counts as dependence, so extraction-on-citation is applied, not excepted. Known holes held by judgment: the prose channel — it-848k [item sketch: `the prose channel: talk precedes writes and the graph never sees it`]. (The second original hole, leaseless work, closed 2026-08-09: dc-cc76 [decision in-force: `the lease is an arc declaration: boundary at first file touch; leaseless writes observed, never denied`] rules the boundary; this clause updated 2026-08-11 when the stale mention was caught.) Protocol content seeds the salvage's day zero.
      decision in force: "the surfacing atom: one struct, three renderers, one module, lint-enforced" — decision [in-force] v2 · cli, process · user (dc-nnf5) — Ruled 2026-08-11 by the user. One atom struct, three renderers, all owned by one surface module; no node field is formatted anywhere else, enforced by lint — front.title outside the module fails wrap or CI. The atom carries: id, title, type, kind, status, v, areas, provenance (with source/method presence for claims), archived. The renderers: atom_line — the full atom on one line, for every list register (find, queue, ready, shaping, touches, homework, brief shelves, hook enumerations); kind rides type, areas render as titles. atom_ref — the floor for every binary-composed sentence (errors, confirmations, refusals): "title" [type status] (id), status unconditional, archived riding the status slot. atom_unpack — body renders expand authored ids in place from the same struct, id-anchored, announcing areas only when the cited node's areas differ from the citing node's: foreign mentions announce themselves, home stays quiet. Registers differ only in how much of the atom they render, never in where they are built. Deliberately excluded: find match hygiene (substring hits — 'cli' inside 'click' admitted dc-qhru [decision in-force: `a game is a plugin pack; the engine ships opinionated plugins; marketplace is opt-in` — areas: deepcraft salvage] as debris) is a separate defect, filed as its own item.
      decision in force: "no engine tags: areas are the vocabulary; the field registry carries experiments" — decision [in-force] v1 · cli · assistant (dc-nwba) — Tags in three registers, three answers (settled 2026-08-09). As an array field: already possible — the project field registry preserves unknown fields verbatim, never interpreted; zero engine work, and per the schema-capped principle the engine interprets nothing new until a real query hurts. As an authority: rejected — areas already are the controlled tag vocabulary (defined, versioned, reviewable); free tags would be a shadow taxonomy riding a remembered convention, the kind measured to die (JUSTIFIED-BY: documented twice, used three times). A tag that proves load-bearing is an area wanting to be born. As matcher fuel: unnecessary — tags would add recall only for pre-nodal concepts, and the reverse match at nodehood closes that gap convention-free. Hashtags in bodies rejected: bodies are title-first prose by ruling; backticks already mark concepts.
      decision in force: "multi-held dispatch: a chat holds many, an item belongs to one chat, the boundary harvests all" — decision [in-force] v2 · cli · user (dc-qyr5) — Multi-held: a chat may hold any number of live dispatches; the same-chat second-dispatch refusal is deleted - stamping follows the work (dc-zbxj [decision in-force: `join-as-fetch: dispatch mints a token, the spawn prompt is one line, q join binds identity and renders the brief; hooks inject identity only; stamping follows the work` — areas: cli, process]), so holding resolves nothing and the ambiguity that justified the refusal is gone. An item belongs to one chat: dispatching an already-live-dispatched item refuses, naming the holding chat and session; re-dispatch from the same chat stays free (the documented recovery flow - fresh token minted, old token dies); from anywhere else, steal with a required reason takes the whole dispatch over, loud and logged - stealable, never shareable, the lease pattern applied to dispatches. Boundary: wrap, resume, and retire refuse while the chat holds ANY live dispatch, enumerating each with its q harvest command - no parking across a boundary; subagents cannot outlive their host session, so the long-runner exception is a non-case. Settles the remaining topics of the findings triage: multi-badge holding and holes 4 and 5.
      decision in force: "prose ids: bodies cite by immutable id, unpacked at render; mentions derive, never store" — decision [in-force] v3 · cli, process · user (dc-wwnk) — Ratified 2026-08-11 (user floated the feature; assistant shape confirmed by the user). Node ids are legal citations in NODE BODIES; conversation with the user keeps titles — the house rule splits, not flips. Rationale: titles are mutable labels, ids immutable anchors. A title reference orphans silently on retitle; an id reference never rots, and render-time unpacking always shows the current title — the blob-stamp design applied to prose. Revives DESIGN.md § 3's unbuilt wikilink provision, with ids instead of slugs. The machinery is DERIVED, never stored: (1) `id-unpack` — every rendered body (open, brief, the view) expands bare ids inline as id [type: `title`], hyperlinked in the UI, dead targets labeled. (2) `mention-index` — a regex scan for the closed id shape ({ar,it,th,dc,cl,do}-xxxx; code spans skipped) derived at index time; open and the view show "mentioned by" backlinks labeled as derived; blast and behind stay real-edge-only (a mention references; it does not lean). (3) `mention-surfaces` — mint and edit echo each resolved id's title beside it (catching the wrong-but-real id, the hallucination case a dangling warning cannot), warn quietly on id-shapes resolving to nothing (never a gate — hyphenated prose false-positives like `th-read` exist), and offer a real-edge upgrade as judgment, never an auto-link. No mentions rel (the reverse channel derives); no auto work items from danglers (wrap lints them). Upgrading a load-bearing mention to a real edge is the recorded act — extraction-on-citation applied to references.
      decision in force: "kinds: work modes for items, material species for claims; engine kindless, defaults seeded" — decision [in-force] v2 · process, cli · user (dc-yd9s) — Ruled 2026-08-13 by the user, closing the species conversation. Item kinds are WORK MODES - the closed set of ways of engaging the material: slice (building), spike (probing - already load-bearing, the queue hides spike-blocked threads), debt (repaying), bug (repairing), watch (watching), arc (the container that never fires itself; its part-of children carry the work). feature retires from items - work units are not product identity; the 25 wearing it re-kind on touch, no sweep. Claim kinds are MATERIAL SPECIES and become explicit: vein (mechanism and mandate, dc-wrnc [decision in-force: `the vein: spines rename into the quarry register`]), feature (the receipt - what it is, does, and why; the fine-grain family member of area, mandatory as the feature index), measured (two temporal species, ruled onward in th-cfmt [thread queued: `measured species: one-off readings vs living measurements - behind must tell sediment from rot`]), contract (the philosophical API of a piece of code, symbol grain; first slice anchors by the backticked symbol name in the title, a symbol field a later candidate per summarizer shaping, th-yzmj [thread queued: `the file-size infection: mid-task warnings fail; what defense actually works`]). Explicit because underivable: vein and receipt share grounding shape. The register splits deliberately: the honor-register for material (veins - prospected, extracted, precious), the plain register for labor (slices - workmanlike). The engine stays kindless-but-kind-aware throughout: it renders, groups, and counts whatever strings exist, never validates. Per-mode and per-species floors are protocol content (slice: acceptance names and write-set; spike: the question it answers and what it informs; debt: the invariant served; bug: repro; watch: trigger; arc: children), made construction by it-vzx6 [item sketch: `protocol triggers cover the choke points: status flips and dispatch`] and it-qra3 [item sketch: `protocol-declared floors: required fields presence-warned, with authored warn text`]. These registers are load-bearing DEFAULTS of the system quarry creates: setup offers to customize them, never whether to have them; scripts bootstrap (th-qv27 [thread queued: `setup runbook: what quarry asks and offers at init time`]). First fallout: it-xzp7 [item shaped: `vein rename sweep: surfaces say vein where they said spine`] sweeps the rename, the shelf-by-claim-kind render, and the one-pass claim-kind assignment - the one deliberate exception to on-touch, so the shelf reads honest immediately. Friction discovered later resolves later - this is the shape to keep working from.
      decision in force: "join-as-fetch: dispatch mints a token, the spawn prompt is one line, q join binds identity and renders the brief; hooks inject identity only; stamping follows the work" — decision [in-force] v1 · cli, process · user (dc-zbxj) — Ruled 2026-08-13 by the user, hashed out in conversation on th-6upm [thread resolved: `per-chat badge findings: dispatcher stamping, single-badge chats, and the five holes`] (first topic of the findings triage). The hand-off is a FETCH, not a paste: q dispatch mints a single-use join token into its output; the spawn prompt collapses to one line (run q join <token> and follow what it prints); q join binds the acting agent identity to the badge and renders the brief fresh from the graph at read time - the brief doctrine (derived, never hand-carried) applied to the hand-off itself, so a stale or dispatcher-mangled copy is impossible and bulk dispatch is N one-line prompts. Anything the dispatcher would have added around the payload belongs in the graph - written to the dispatched item if that is where it belongs - so the derived brief carries it.
      spine: "`join-gate`: writes into a dispatched lease from a context resolving no badge deny with the teaching line naming q join" — claim [asserted] v2 · cli · assistant·sourced (cl-2pc9)
        File writes into globs held by a live dispatch from a context resolving no badge are denied with a teaching line naming the item and q join — the C8 move applied to the hand-off. The holder session and the unbound context are taught the join (this replaces the holder-session Allow for dispatched zones); a genuinely foreign session keeps the C7 denial; solo leases (reserve without dispatch) keep their holder Allow and scope-creep warn, and the dispatcher chores outside a dispatched zone stay ordinary leaseless observation.
      spine: "`surface-atom`: one struct in one module — `atom-line`, `atom-ref`, `atom-unpack` own every register" — claim [asserted] v2 · cli · assistant·sourced (cl-3qwu)
        One Atom struct resolved from the graph carries id, title, type, kind, status, v, areas as titles, provenance with grounding presence, and archived; atom_line, atom_ref, and atom_unpack are the only renderers, all owned by src/surface.rs.
      spine: "`session-kind-field`: kind is registry data — one validation point in `parse_kind`, surfaces render what they find" — claim [asserted] v4 · cli · assistant·sourced (cl-3rx9)
        registry entries carry an optional kind set at q session set --kind; coord::parse_kind is the only place the string is judged (open set: a new kind is one new arm there, never a sweep); q session list, q session resume, and the SessionStart orient render whatever kind an entry carries beside the charter, and kindless entries render exactly as before the field
      spine: "`q-serve`: the view rendered per request over the live store" — claim [asserted] v3 · cli · assistant·sourced (cl-4k9e)
        each GET re-renders the whole view over the live store: std-only accept loop on 127.0.0.1:7171 (--port), no cache, no watcher, no new dependency; render errors answer 500; the baked write stays the offline courtesy and q view --open prefers the served URL when the loop answers a probe
      spine: "`q-join`: dispatch mints a single-use token into a one-line spawn prompt; q join consumes it, binds the acting identity to the badge, and renders the brief fresh" — claim [asserted] v2 · cli · assistant·sourced (cl-6ctr)
        q dispatch mints a single-use join token into a one-line spawn prompt; q join consumes the token, binds the acting identity (agent, chat, or session key) to the badge in the association map, logs the join, and renders the brief fresh from the graph. Re-join by the same identity is idempotent; a second identity refuses; a re-dispatch mints a fresh token and kills the old one.
      spine: "`find-areas`: find hits ride atom_line with areas labeled" — claim [asserted] v2 · cli · assistant·sourced (cl-89b5)
        find hits render atom_line, so kind, areas, provenance, and archived arrive labeled on every match and foreign-domain hits name themselves at arrival.
      spine: "`work-only-stamping`: held entries resolve refusal and boundary only; stamping resolves env and association, never the holding chat" — claim [asserted] v3 · cli · assistant·sourced (cl-9nxr)
        Held dispatch entries resolve refusal and boundary only (boundary_badge reads env, associations, then held); badge resolution for stamping and the write guard (badge_for) reads env and the association map by agent, chat, then session key — never the holding chat or session. The dispatching chat mid-flight acts stamp nothing; no-attribution beats mis-attribution.
      spine: "`boundary-harvests-all`: wrap and resume/retire refuse under any live held dispatch, enumerating each with its q harvest command" — claim [asserted] v4 · cli · assistant·sourced (cl-ahzk)
        wrap and session resume/retire refuse while the chat holds ANY live dispatch - env badge, joined association, or any held entry of this chat - and the refusal enumerates every held dispatch with its q harvest command instead of first-found; a chat with no badge keeps its boundary verbs while other chats' dispatches fly. No parking a dispatch across a boundary.
      spine: "`kind-shaped-wake`: the wake shape derives from session kind at one match point - dispatch wakes into ready, in-flight with harvest commands, and homework residue" — claim [asserted] v3 · cli · assistant·sourced (cl-br3p)
        dispatch-kind sessions wake into what a dispatcher owes: coord::wake_shape is the one kind match point (catch-all keeps the generic brief for design, kindless, and unknown kinds); render::dispatch_wake is one renderer consumed by both wake surfaces (q session resume and the SessionStart orient) - ready in purview leads, each in-flight item carries its q harvest command, and homework residue closes (stale refs capped at five, unharvested dispatches the in-flight shelf no longer shows); the owed-threads block is omitted under the dispatch shape (threads are not a dispatch session's to settle, dc-wngq [decision in-force: `the dispatcher: quarry's dispatch session - durable, all-areas, named plainly` — areas: process]); design and kindless wakes render exactly as before.
      spine: "the area gate and per-area watermarks deliver read-firsts and foreign d…" — claim [measured] v3 · cli · measured·sourced·method (cl-c34g)
        the area gate and per-area watermarks deliver read-firsts and foreign drift at write time
      spine: "`mention-surfaces`: mint and edit echo resolved titles, ask about danglers, offer the edge upgrade; wrap lints" — claim [asserted] v3 · cli · assistant·sourced (cl-c7q2)
        new/edit/claim/rule confirmations echo each resolved id's current title beside it (the echo is the verification a dangling warning cannot give — a wrong-but-real id reads wrong); id-shapes resolving to nothing ask a question, never gate; the real-edge upgrade is offered as judgment, never auto-linked; wrap lints danglers
      spine: "`mention-index`: mentioned-by backlinks derive from the body scan at render; blast and behind stay real-edge-only" — claim [asserted] v3 · cli · assistant·sourced (cl-cagx)
        q open and the view list mentioned-by backlinks derived at render from the body scan, labeled distinct from edge backlinks; nothing is stored, no mentions rel exists; blast and behind stay real-edge-only — a mention references, it does not lean
      spine: "mint-time relatedness matches the title lexicon forward and nodehood re…" — claim [measured] v3 · cli · measured·sourced·method (cl-en99)
        mint-time relatedness matches the title lexicon forward and nodehood reverse, for judgment
      spine: "`q-harvest`: landing renders observed-vs-leased, advertises the dispatc…" — claim [asserted] v3 · cli · assistant·sourced (cl-enu7)
        `q-harvest`: landing renders observed-vs-leased, advertises the dispatch trace, and registers the report in one command
      spine: "`intent-delta`: acceptance names join spine titles per shared area — unlanded intent and emergent scope, both directions" — claim [asserted] v4 · cli · assistant·sourced (cl-gu5f)
        `intent-delta`: backtick-named acceptance lines of live items join spine claim titles per shared area, both directions, advertised at shaping and area open
      spine: "`touched-set-accrual`: the guard observes every in-repo write into mach…" — claim [asserted] v3 · cli · assistant·sourced (cl-h2gx)
        `touched-set-accrual`: the guard observes every in-repo write into machine-local JSONL, per-session leaseless and per-item under a badge, with no graph load on the write path
      spine: "`q-claim`: grounded extraction in one act — C1 subjects, C2 grounding, derived provenance, spine titles ride --title" — claim [asserted] v2 · cli · assistant·sourced (cl-hd56)
        `q-claim`: extraction-on-citation as one verb — C1 demands a subject (--about area or file: target), C2 demands grounding for non-user claims (--source doc or file:path, --method, or user provenance; no free-floating assertions), provenance derives (explicit flag, else method implies measured, else actor), the title derives from the first line at 72 chars unless --title carries the register-length spine name, and the source edge lands with the mint
      spine: "`claim-title-override`: q claim --title carries register-length spine names past the 72-char derived cut, no retitle two-step" — claim [asserted] v3 · cli · assistant·sourced (cl-jgnj)
        The truncation friction the builds-on dispatch reported is closed: q claim --title carries a full register-length title while the text stays the body; the derived first-line cut remains the default. This claim's own title exceeds seventy-two characters as its living proof.
      spine: "`charter-at-wake`: q session resume and the SessionStart orient render the bound session's charter beneath the purview line" — claim [asserted] v3 · cli · assistant·sourced (cl-mk8b)
        q session resume prints the registered charter beneath the purview line, and the SessionStart orient resolves the wake session env-first then chat-binding (an adopted chat orients as its session) and renders the same charter_line text beneath the session purview line; sessions without a charter wake exactly as before
      spine: "`q-dispatch`: one act renders the brief, reserves, sets in-flight, and…" — claim [asserted] v3 · cli · assistant·sourced (cl-p6aj)
        `q-dispatch`: one act renders the brief, reserves, sets in-flight, and prints the badge-carrying hand-off payload
      spine: "the dispatch chain gates reserve on briefs and holds code writes to lea…" — claim [measured] v4 · cli · measured·sourced·method (cl-qm6t)
        the dispatch chain gates reserve on briefs and holds code writes to leases
      spine: "`id-unpack`: rendered bodies expand bare ids to the current title, dead targets labeled, the view hyperlinked" — claim [asserted] v5 · cli · assistant·sourced (cl-qxxp)
        every rendered body (open, brief, the view) expands each bare id to id [type: `title`] with the target's current title; refuted/superseded/dropped and archived targets carry their label; the view hyperlinks to the node anchor; backticked code spans are skipped; dangling shapes stay as written
      spine: "`q-unlink`: edge retirement is a logged act - frontmatter updated, no v…" — claim [asserted] v3 · cli · assistant·sourced (cl-rv66)
        `q-unlink`: edge retirement is a logged act - frontmatter updated, no version bump on either node, actor and note on the event
      spine: "`multi-held-dispatch`: held entries key by item with a holder field - a chat holds many, an item belongs to one chat, steal takes it whole" — claim [asserted] v4 · cli · assistant·sourced (cl-s98g)
        held dispatch state keys by item with the dispatching chat riding each entry as holder; a chat holds any number of live dispatches (the same-chat second-dispatch refusal is deleted); dispatching an item another chat holds live refuses naming the holding chat and session; same-chat re-dispatch stays free with a fresh token; q dispatch --steal with a required --reason takes the dispatch whole - held entry, lease, fresh token, old agent's associations cleared - loud and logged. Legacy single-slot and per-chat-keyed state files migrate in place.
      spine: "`atom-lint`: raw title access outside the surface module fails wrap and CI" — claim [asserted] v2 · cli · assistant·sourced (cl-syj7)
        The literal front-title token outside src/surface.rs fails tests/surface_lint.rs at CI, and q wrap runs the same scan when the graph root carries quarry sources; matching and mutation ride title_raw and retitle, so the ban is total and needs no judgment about which reference is formatting.
      spine: "`carrier-replumb`: Behind and homework carriers hold atoms" — claim [asserted] v2 · cli · assistant·sourced (cl-uqqn)
        queries::Behind carries its source and node targets as Atoms; homework lines, wrap, and the brief behind-check print from those atoms, so no starved id-title pair survives in the carrier layer.
      spine: "`builds-on-edge`: q link records builder-to-built-upon lineage, stamped and status-free; behind reports it, blast walks it in reverse" — claim [asserted] v4 · cli · assistant·sourced (cl-vqdb)
        `builds-on-edge`: q link records builder-to-built-upon lineage (decision-to-decision, doc-to-claim/decision), stamped like any edge, coupling no status; behind reports it and blast walks it in reverse
      spine: "`agent-identity-injection`: the session hook injects QUARRY_AGENT from the hook-provided agent_id alongside CHAT/SESSION/ACTOR; badges resolve from the association map" — claim [asserted] v2 · cli · assistant·sourced (cl-z6gc)
        The session hook reads the agent id the harness provides to hooks run in a subagent (top-level agent_id, verified live 2026-08-13; read defensively across key spellings) and injects QUARRY_AGENT alongside QUARRY_CHAT, QUARRY_SESSION, and QUARRY_ACTOR. q resolves badges from the association map by agent, chat, then session key; the per-shell badge export died from the dispatch payload and QUARRY_DISPATCH env survives only as the out-of-hook-coverage override.
      doc: "dispatch report: session kind as registry data: --kind on set, one validation point, open set" — doc·report [registered] v2 · cli · assistant (do-2hcu) · docs/reports/2026-08-13-session-kind-field-it-skpa.md
      doc: "dispatch report: join-as-fetch lands" — doc·report [registered] v2 · cli · assistant (do-3kyh) · docs/reports/2026-08-13-join-as-fetch-it-vkxh.md
      doc: "dispatch report: per-chat badge lands" — doc·report [registered] v2 · cli · assistant (do-75ut) · docs/reports/2026-08-12-per-chat-badge-it-u8uf.md
      doc: "dispatch report: the surfacing atom lands" — doc·report [registered] v2 · cli · assistant (do-ev5g) · docs/reports/2026-08-12-surfacing-atom-it-ygw7.md
      doc: "dispatch report: the builds-on edge and the intent delta query" — doc·report [registered] v3 · cli · assistant (do-hvmd) · docs/reports/2026-08-10-builds-on-intent-delta-it-tz94.md
      doc: "dispatch report: prose ids land and light up retroactively" — doc·report [registered] v3 · cli · assistant (do-hvup) · docs/reports/2026-08-11-prose-ids-it-w5yv.md
      doc: "dispatch report: multi-held dispatch lands" — doc·report [registered] v2 · cli · assistant (do-jahd) · docs/reports/2026-08-13-multi-held-it-xpt7.md
      doc: "dispatch report: fast follows land; the fence catches its builder" — doc·report [registered] v2 · cli · assistant (do-jn4s) · docs/reports/2026-08-11-fast-follows-it-69mn.md
      doc: "dispatch report: q serve: the view rendered per request" — doc·report [registered] v2 · cli · assistant (do-q4qf) · docs/reports/2026-08-12-q-serve-it-kxdu.md
      doc: "dispatch report: recent activity carries type and area" — doc·report [registered] v2 · cli · assistant (do-rck7) · docs/reports/2026-08-12-activity-columns-it-5j34.md
      doc: "dispatch report: kind-shaped wake: a dispatch session resumes into ready, in-flight, and homework" — doc·report [registered] v2 · cli · assistant (do-sdcb) · docs/reports/2026-08-13-kind-shaped-wake-it-wub5.md
      doc: "quarry design document" — doc [registered] v1 · schema, storage, cli, process, ui · assistant (do-umyy) · docs/DESIGN.md
      doc: "dispatch report: orientation consumes the charter: wake surfaces carry the session's kind" — doc·report [registered] v2 · cli · assistant (do-uvx6) · docs/reports/2026-08-13-charter-at-wake-it-sumw.md
      doc: "dispatch report: unpack click-target: the whole expansion is the link, and reads as one" — doc·report [registered] v2 · cli · assistant (do-ve3g) · docs/reports/2026-08-12-unpack-click-target-it-6gj9.md
      doc: "dispatch report: the dispatch chain lands" — doc·report [registered] v3 · cli · assistant (do-xt6f) · docs/reports/2026-08-10-dispatch-chain-it-7ss9.md

WRITE-SET:
  leaseless — research dispatch; DO-NOT-TOUCH: every file. To write code, the dispatcher reserves first: q reserve it-hapc --files <globs>
  (the whole chain in one act — brief, lease, in-flight, hand-off payload: q dispatch it-hapc --files <globs>)

RETURN SPEC (accept by outcome):
  ⚠ no acceptance recorded — outcomes cannot be judged. Fix the graph first: q set it-hapc acceptance+="..."
  REFLECTIONS (always): close the report with doubts, surprises, and design friction in your own words — candor beats polish; reflections are mined afterward.
  STOP-REPORTS: stopping before acceptance is met is a valid outcome — say so explicitly (why, where you stopped, what remains) and the dispatcher re-dispatches from your report. A partial report registers like any other.

PROTOCOL — "resource rules: this machine's build serialization" [doc registered] (do-799m):
  Build rules for this machine — violating these has hung this box:
  · cargo: ~/.cargo/bin is on the User PATH; if bare `cargo` fails in your harness, use the full path ("$HOME/.cargo/bin/cargo" bash · "$env:USERPROFILE\.cargo\bin\cargo.exe" pwsh).
  · TMP/TEMP→B:\tmp and jobs=4 are owned by ~/.cargo/config.toml — do not override them; C: is space-constrained and a full drive fails the linker with LNK1108.
  · ONE build at a time, never concurrent cargo processes — this machine has hung under parallel heavy builds.
  · Verify tests by READING the raw `test result:` line and its counts, never through a grep filter — a filter once silently swallowed a pass line ("0 passed" is a substring of "20 passed").
  · This repo's hooks invoke target/release/q.exe: while cargo rebuilds it the exe is being replaced, so a transient hook error mid-build is the build, not the graph — run q verbs outside build windows.

ACTOR RULES:
  · Build from spine: the claims above are `load-bearing capabilities` — design from these bones before proposing new structure.
  · All graph writes go through q verbs; your work logs under QUARRY_ACTOR (auto-injected).
  · C3: never settle or supersede user-provenance nodes. When the work hits a call that is the user's — a fork that would contradict or overturn a user ruling — queue a thread, take the least-committal provisional path consistent with standing rulings, and keep building; flag the provisional call in your report. The thread surfaces the call; it never blocks your arc.
  · Cite what you build on (q link ... / q claim --source ...); harvest is judged from the diff, not the report.
  · Landing belongs to the dispatcher: report against the RETURN spec and stop — never flip status=done, never release the lease. "Done" from an agent is a stop signal, not a transition; the dispatcher judges at: q harvest it-hapc
