---
id: it-am2r
type: item
title: the write-set shaping station takes a comma-joined glob the fire-time floor will later refuse
v: 2
status: sketch
provenance: assistant
created: 2026-08-21T09:59:57Z
actor: claude-fable-5
kind: debt
edges:
- rel: about
  to: ar-c7f5
  at: 1
---

Flagged from the agent seat at the it-x4bb landing, 2026-08-21, and deliberately left out of that arc's scope — recorded here so the decision is a choice rather than an oversight.

coord::check_glob_shapes now refuses a comma-joined --files value at both lease-taking stations (ops::dispatch on the flag, coord::reserve on whatever reaches it). q set <item> write-set+="a,b" still accepts the shape. Nothing breaks: a dispatch that falls back to a recorded write-set passes it through reserve, which refuses before any lease exists, so no agent can ever be denied its own write-set by this road. The load-bearing floor holds.

What remains is only WHERE the error arrives. A write-set authored with a comma sits quietly correct-looking in the item until someone fires a dispatch against it, and the refusal then names a value the firing dispatcher did not type and may not have written. Catching it at the authoring act would put the message next to the hand that made it.

Filed as debt, not a bug, on the agent's own reasoning: the fire-time floor is the one that protects an agent mid-arc, and it holds. Adding a refusal at a SHAPING station is a seat question — it-x4bb did not open it, and this session's pen is not design's. The mechanical work is one call; whether a shaping verb should refuse a shape at all is the part worth a design word.

Neighbourhood: it-x4bb for the floor this leans on, cl-74qt for why refusing beats splitting.
