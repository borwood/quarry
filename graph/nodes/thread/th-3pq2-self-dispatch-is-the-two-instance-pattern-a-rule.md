---
id: th-3pq2
type: thread
title: 'self-dispatch: is the two-instance pattern a rule wanting structure?'
v: 2
status: resolved
provenance: user
created: 2026-08-12T10:48:33Z
actor: claude
archived: true
edges:
- rel: about
  to: ar-xa38
  at: 1
---

Opened 2026-08-12 by the user, after the surfacing-atom dispatch (do-ev5g): the main session dispatched it-ygw7 and then built it itself — dispatcher and agent fused, a first for this repo. Prior practice (main session as dispatcher and design partner, implementation handed to subagents so threads and parallel dispatch stay unblocked) lived in prior chats and in the dispatch reports (do-xt6f, do-hvmd, do-jn4s), but never crossed a surface this session read: the reports rode the brief only as shelf lines, dc-kpqg surfaced once as a find hit, and the chat began from a context clear — the continuity that carried the norm was exactly what got wiped. The house thesis demonstrated on itself: a convention held in memory and prior practice is a bet that it won't happen.

What filled the gap, per the user's session audit: "get it moving" read as build-now-in-loop; the session had just loaded a deep code survey and handing off felt like re-transferring held understanding (the context that makes a good dispatcher makes it feel like the best-positioned implementer — a real, unguarded pull); the harness defaults to direct tool use; and the payload's reassurance that the write guard reads the machine-local badge reads, to a self-directed session, as permission to proceed in place. The session's addendum: the hand-off payload prints into the dispatcher's own terminal addressed to "the agent" — the surface assumes a hand-off happened and never asks whether one did. The fusion was conscious, not an oversight of the machinery; what was missing was any signal that it contradicted practice.

The ruling to make: is the two-instance model a rule, a default with a gated exception, or merely a habit this repo outgrew? If a rule, it wants structure, not memory — candidates: a decision node the brief's read-first would carry; an on=dispatch protocol rider; or an engine-native loud path where self-dispatch demands a --self flag and a reason, the way a steal does. Adjacent and already filed: it-9p3v — the badge is machine-scoped, so dispatcher and agent are indistinguishable to every choke point that exists today; any structural answer here likely leans on that item landing first.
