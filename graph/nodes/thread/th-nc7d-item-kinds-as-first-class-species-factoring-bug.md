---
id: th-nc7d
type: thread
title: 'item kinds as first-class species: factoring bug, debt, watch into the discovery surfaces'
v: 5
status: queued
provenance: assistant
created: 2026-08-11T06:06:58Z
actor: claude
edges:
- rel: about
  to: ar-xa38
  at: 1
- rel: about
  to: ar-c7f5
  at: 1
---

Opened 2026-08-11. The unified shape is user-agreed in conversation: everything is an item at notice; kind names the species (bug, debt, watch); status is the deferral; titles are lexicon anchors. Open for settling: how kinds factor into discovery surfaces per the philosophy - shaping grouping (it-xu9g holds the first cut), whether ready/queue/orient treat kinds differently, whether the view maps them distinctly, and whether kind vocabulary is engine-suggested or purely project protocol (dc-9hgd pressure). The conversation shape is agreed; the surfaces are the ruling to make.

State 2026-08-11: claims evidence entered - the 15-claim census shows the species question is not item-only. Three claim species already cohabit on proxies (spine claims sourced to code; measured claims with method; doctrine claims sourced to docs), and a fourth - symbol-grain contract claims - waits on its consuming surface (the summarizer sketched in th-yzmj). Principle proposed in conversation: kind-on-consumption - a kind earns first-class use when a surface filters or treats by it. Governance recommendation pending the ruling: engine stays kindless-but-kind-aware (groups and counts whatever strings exist, never validates); vocabulary lives in project protocol; setup offers seed packs (bears on th-qv27). User pushback to carry into the ruling: setup-determined kinds without by-construction enforcement rely on discipline, and 'discipline means bet that it won't happen' - construction can hold presence-checks at choke points and demand-side visibility (contract holes shown on briefs), never creation itself; the cold start remains a wager, but one that fails visibly rather than silently.

State 2026-08-13: the conversation converged on consumers and construction; the vocabulary itself stays UNRULED by explicit user marker. Consumers named per species class: the dispatching session's orientation (dc-ydvb) consumes buildable kinds and never watches; shaping grouping (it-xu9g, collapse set as per-surface protocol config) is consumption-proven; per-kind artifact floors ride protocol content, made construction by two generic engine features now sketched - trigger coverage at status flips and dispatch (it-vzx6) and declared required fields with authored warn text (it-qra3). Claim kinds reserved: contract is the first explicit claim kind - contract-ness is an authored designation (selection, normativity, commitment; derivation drafts, ratification binds) underivable from grounding shape - and its consumer is the summarizer (th-yzmj), an engine consumer beside protocols; the authoring process (contract register) is protocol content, dogeared on th-qv27. Setup transport per the user: offer stands, scripts bootstrap the preferred defaults, custom is hand-written - recorded on th-qv27. Kinds stay mobile (it-u8uf traveled watch to debt on a ruling). Archived singleton kinds (slice, spike) stand as sediment; the two live ones (arc on it-wkcq, process on it-vztp) are re-judged at next touch. OPEN and in conversation: the arc/slice vs feature philosophy - items as units of work vs items as capability representations; the capability layer as spine claims; whether the fragmentation of capability state across done items is answered by the spine chain or wants more.

State 2026-08-13, later: the claim-species half settled in conversation. The user's taxonomy: VEIN (mechanism and mandate; renamed from spine by dc-wrnc, sweep filed as it-xzp7), FEATURE receipt (what it is, does, and why - the fine-grain family member of area, mandatory as the feature index; no planned-feature that flips to built, the decisions and work are other nodes; receipt-as-claim over sub-area because features die and areas are the map, with area-birth as the promotion valve per dc-nwba), MEASURED (two temporal species - one-off readings that go behind by design and living measurements wanting re-verification; ruled to its own thread th-cfmt), and CONTRACT (the philosophical API of a piece of code, symbol grain, source and about file; first-slice anchor is the backticked symbol name in the title, a symbol field as a later candidate depending on summarizer shaping - the compromise to carry into the eventual ruling). These registers are load-bearing DEFAULTS of the system quarry creates: setup offers to customize them, never whether to have them. Pattern invariants without a mechanism are decisions, not veins; when the invariant grows teeth the teeth get the vein (the atom-lint precedent: decision, mechanism, vein). Veins can cite veins through claim-to-claim supports. Spikes noted load-bearing beyond vocabulary: the queue derivation already hides threads blocked on spike items - the probe mode is machinery, not just a name. Remaining for the item vocabulary: the work-unit species set (work-unit names, not feature; spike wanted) - the last open topic of the conversation.
