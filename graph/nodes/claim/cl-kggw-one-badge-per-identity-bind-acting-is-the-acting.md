---
id: cl-kggw
type: claim
title: '`one-badge-per-identity`: bind_acting is the acting map sole writer - a live binding never re-points; join refuses loud, the env road declines quiet'
v: 2
status: ratified
provenance: assistant
created: 2026-08-20T11:01:18Z
actor: claude
kind: vein
ratified:
  by: claude
  date: 2026-08-20
edges:
- rel: about
  to: ar-c7f5
  at: 1
- rel: source
  to: file:src/coord.rs
  at: 43831a2620af
---

coord::bind_acting is the ONE writer of the acting map (it-tanf, user-ruled 2026-08-20): an identity binds at most one LIVE badge, held by construction - a standing live binding is never overwritten, so the sequential multi-join overwrite that made every act stamp the last join (the lexicon-trio incident, dc-qvtz sitting) is dead, not discouraged. The join road (coord::consume_join_token) refuses a second live badge loudly BEFORE the single-use token is spent: the refusal names the live badge and the roads out - report and stop so the dispatcher harvests, or separate dispatches, or the bundle shape when th-zzqv lands - and leaves the token live for the right agent; re-join of the identity own badge stays the idempotent read, and now also restores a lost acting row (the pin-restore pattern). The env road (coord::record_acting via note_acting) declines the learn silently - per-shell QUARRY_DISPATCH still stamps env-first, so nothing is lost where the override is deliberate. coord::live_acting_badge is the read side: a row pointing at a cleared dispatch is residue, never a binding, so harvest frees the identity by construction. Attention rides along with no extra machinery (the it-csm3 caveat on cl-b2z2): attention_key resolves through the same acting map, so a badge-scoped reader can no longer inherit a last-join blur. Extends cl-6ctr; th-78uc self-join leans on this refusal being true. Pinned by join_refuses_a_second_live_badge_naming_the_roads_out and acting_map_holds_one_badge_per_identity_by_construction in tests/basic.rs.
